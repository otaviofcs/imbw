%h3 Mural de Recados
- if comments.size > 0
  - comments.each do |comment|
    = render :partial => 'gallery_comments/comment', :locals => { :comment => comment }
- else
  %p Seja o primeiro a comentar.
