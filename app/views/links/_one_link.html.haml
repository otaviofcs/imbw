.link
  %p
    = link.title
    %br/
    = link.description
    %br
    = link_to "link", link.link
  %p
    tags:
    - link.tags.each do |tag|
      = link_to tag.name, links_path(:search => { :tagged_with_on_tags => tag.name })