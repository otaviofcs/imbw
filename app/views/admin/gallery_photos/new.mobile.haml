.general
  %h2
    Nova foto para o álbum
    = @gallery.title

  %p
    = link_to "< ver todo o álbum #{@gallery.title}", admin_gallery_path(@gallery)
  - form_for @photo, :url => admin_gallery_photos_path, :html => { :multipart => true } do |f|
    = f.error_messages
    %p
      = f.label_t :title
      %br
      = f.text_field :title, :class => 'full'
    %p
      = f.label_t :uploaded_data
      %br
      = f.file_field :uploaded_data, :class => 'full'
      %br
      %span.field_info Arquivos .png, .jpg. ou .gif, com até 3mb de tamanho
    %p
      = submit_tag 'Enviar', :disable_with => 'salvando...', :class => 'button'
      ou
      = link_to 'cancelar', admin_gallery_path(@gallery)