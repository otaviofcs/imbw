%h2 Posts
%p
  = link_to "novo post", new_admin_post_path, :class => 'link-new'
%table
  %tr
    %th artigo
    %th última edição
    %th #
  - @posts.each do |post|
    %tr
      %td= link_to post.title, edit_admin_post_path(post)
      %td= link_to l(post.edited_at, :format => :short), edit_admin_post_path(post)
      %td
        = link_to "ver", post_path(post), :class => 'link-show'
        ou
        = link_to "excluir", admin_post_path(post), :method => 'delete', :confirm => 'Tem certeza que deseja excluir este item? Não será possível voltar atrás.', :class => 'link-delete'