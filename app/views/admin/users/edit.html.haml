%h4
  Editando Usuário #
  = @user.id
.data_edit
  - form_for @user, :url => admin_user_path(@user), :method => :put do |f|
    = render :partial => 'admin/users/fields', :locals => { :f => f }
    %p
      = submit_tag 'Enviar', :disable_with => 'salvando...', :class => 'button'
      ou
      = link_to 'cancelar', admin_users_path